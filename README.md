# In order to make the app work first install the dependencies by running the following command  

```
npm install
```

# Start the server by typing the following command

```
node app.js |or| nodemon app.js
```
